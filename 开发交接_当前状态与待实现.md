# 小丑鸭｜开发交接：当前状态与待实现

本文是当前代码交接的统一入口，状态更新至 **2026-09-23**。新成员先按“运行检查”确认底座，再依次完成 P0～P3；不要先重画页面或重写已经打通的多模态、记录与桌面组件功能。

## 1. 项目目标与当前主线

小丑鸭不是打卡工具，而是帮助缺乏自信的用户在自然聊天中逐渐看见自己。完整主线为：

```text
文字 / 图片 / 语音自然交流
→ AI 先回应当下感受
→ 独立提取有长期价值的成长事件
→ 分层记忆并保留原始来源
→ 在后续对话中引用真实经历
→ 必要时建议向老师、同学、朋友等求助
→ 生成日 / 周 / 月回望
→ 小组件与成长小片呈现真实变化
```

产品原则：不催促打卡、不把停顿视为失败、不编造成长证据、不替用户自动联系他人。

## 2. 已完成并验证

### Android

- Kotlin + Jetpack Compose + Material 3，`minSdk 26`。
- 首页、成长、记录、我的四个主页面和底部导航。
- 小丑鸭角色图片、统一配色、字体与卡片组件。
- 文字聊天已接 FastAPI。
- 图片选择与图片理解已接真实模型。
- 轮次式语音：录制 16 kHz 单声道 WAV → 上传 → 转写 → 回复 → 回写首页会话。
- 语音朗读：`Serena` 温柔女声、`Ethan` 温暖男声、手机系统声。
- “我的—调整陪伴方式”可选择音色、试听、开关自动朗读和提示音；云端 TTS 失败自动回退系统声。
- 首次启动可通过语音自然介绍自己，模型按必需字段继续追问；完成后生成本地用户画像并在记忆中心以气泡展示。
- 记录页“写一句 / 说一句 / 拍一张”均可用；语音同时保存原始 WAV 与转译文字，照片支持相机、相册和感受批注。
- “最近留下的”支持点击查看：文字看全文、照片看大图与批注、语音播放原声并查看转译。
- 首页与“我的”共用用户画像称呼；陪伴设置、记忆气泡和记录详情弹窗已统一为米白圆角风格。
- 三个 Glance 桌面小组件已可添加：今日成长、本月足迹、快速记录；写/说/拍可进入记录页对应模式。
- 已加入冷启动封面、Android 12 系统闪屏和小鸭桌面图标。

### FastAPI

- 配置、路由、Schema、Service 分层已经建立。
- `GET /health`。
- `POST /api/v1/chat`：真实文字聊天。
- `POST /api/v1/multimodal/image`：JPEG/PNG/WebP，最大 8 MB。
- `POST /api/v1/multimodal/audio`：WAV/MP3/AAC/AMR/3GP，最大 6 MB。
- `POST /api/v1/multimodal/speech`：`qwen3-tts-flash`，仅允许 `Serena`/`Ethan`，返回音频字节。
- 成长事件、支持建议和周期回顾已有稳定 Mock 接口。
- 首次画像语音追问、本地画像 JSON、记忆中心气泡展示已经完成。
- 记录页文字、原声语音与转写、相机/相册、照片批注和本地保存已经完成。
- 三个 Glance 桌面小组件及记录页深链已经完成。

### 验证记录

- Android `:app:compileDebugKotlin` 成功。
- Python `compileall` 成功。
- `Serena`、`Ethan` 均真实返回 WAV。
- 图片理解、语音转写和聊天真实链路已分别走通。
- 小组件 Provider 已在真机注册，三个点击 PendingIntent 均能成功发送。

## 3. 运行检查

后端：

```powershell
cd D:\python\AI_creative\confidence_agent\server
python -m uvicorn app.main:app --host 0.0.0.0 --port 8000
```

另开终端：

```powershell
Invoke-RestMethod http://127.0.0.1:8000/health
```

真实模型环境应看到 `status=ok`、`mock=False`、`ai_configured=True`。

USB 真机：

```powershell
D:\jdk-11.0.28\platform-tools\adb.exe reverse --remove-all
D:\jdk-11.0.28\platform-tools\adb.exe reverse tcp:8000 tcp:8000
D:\jdk-11.0.28\platform-tools\adb.exe reverse --list
```

本机直接执行 `adb` 可能调用雷电模拟器版本，因此使用以上完整路径。

密钥约定：真实 Key 只放在 `server/.env`；GitHub 只提交 `server/.env.example`。不要把 Key 放进 Android、`BuildConfig`、截图、日志或 Markdown。

## 4. 当前缺口

| 模块 | 当前情况 | 优先级 |
|---|---|---|
| 会话历史 | 仅保存在内存，重启 App 消失 | 主干 |
| 对话上下文 | 模型主要收到当前一句 | 主干 |
| 长期记忆 | 有 Schema/设计，无数据库闭环 | 主干 |
| 成长时间线 | 记录页已能本地保存文字、原声语音、转写、照片和批注，但尚未进入成长页 | 主干 |
| 一起向前 | Mock 建议，支持圈未落库 | 主干 |
| 日周月回望 | Mock 数据 | 主干 |
| 来源追溯 | UI 有入口，尚不能跳回原记录 | 主干 |
| 成长小片 | 只有入口 | 展示增强 |
| 桌面小组件 | 三种基础组件和深链已实现；后续接真实事件、隐私筛选与刷新策略 | 展示增强 |
| 全双工语音 | 当前为按轮录音 | 本阶段不做 |

## 5. 队友实施任务

### P0：持久化和上下文对话

目标：重启后历史仍存在，小鸭能理解连续对话。

1. 在 `server/app` 增加 SQLAlchemy model、repository 和数据库初始化。
2. 建议表：
   - `conversations(id, device_id, created_at, updated_at)`
   - `messages(id, conversation_id, role, modality, content, media_ref, created_at)`
   - `growth_events(...)`
   - `memories(...)`
   - `support_people(...)`
   - `reviews(...)`
3. `/chat` 写入用户消息和助手回复；生成前读取最近 12 条消息。
4. 新增 `GET /api/v1/conversations/{device_id}/messages`。
5. Android 增加 Room 作为本地缓存，启动时先显示缓存，再与服务端同步。
6. 语音转写和图片说明也写入同一 `messages` 表，`modality` 分别为 `audio`、`image`。

注意：现有 API 字段只能增加可选字段，不能直接改名，否则会破坏当前 Android。

验收：连续追问能引用本轮真实上下文；杀掉 App 再打开仍能看到历史。

### P1：成长事件与分层记忆

目标：聊天自然进行，系统在后台判断什么值得记住。

每轮结束后独立调用结构化提取，不要让生成回复的 Prompt 直接写数据库。建议输出：

```json
{
  "fact": "完成了答辩开场练习",
  "feeling": "仍然紧张",
  "attempt": "练习了30秒",
  "support_received": "室友陪练",
  "people": ["室友"],
  "confidence": 0.91,
  "value_score": 0.78,
  "sensitivity": "low",
  "memory_decision": "long_term",
  "source_message_ids": [31, 32]
}
```

采用“模型提取 + 程序规则裁决”：

```text
value_score = 0.30×长期价值 + 0.25×成长意义 + 0.20×事实具体度
            + 0.15×未来可复用性 + 0.10×支持关系价值

低敏感且 score ≥ 0.72  → 自动进入长期记忆
0.52 ≤ score < 0.72      → 进入当日摘要草稿
score < 0.52             → 不保存
敏感、矛盾或低置信度     → 询问用户后再保存
```

不要每句话都询问。普通、高置信、低敏感信息可静默收录；敏感信息、与旧记忆冲突的信息必须确认。

建议新增：

- `GET /api/v1/memories`
- `PATCH /api/v1/memories/{id}`
- `DELETE /api/v1/memories/{id}`
- `POST /api/v1/memories/{id}/confirm`
- `GET /api/v1/events`

检索流程：最近消息 12 条 + 关键词/向量召回 10 条 → 相关度、重要度和时间衰减重排 → 最多取 4 条记忆。每条证据必须有来源 ID、日期和类型；无证据时禁止生成“你上周练习了三次”一类具体事实。

验收：能引用三天前的真实经历；点击“查看来源”回到原记录；删除后不再引用。

### P2：“一起向前”支持闭环

目标：除了鼓励用户自己努力，也能适时建议向可信的人求助。

1. “我的—支持圈”维护称呼、关系、适合帮助的场景；不自动读取通讯录。
2. 用户持续受阻、主动求助或任务难度过高时，策略设为 `seek_support`。
3. `/support/suggest` 返回：推荐对象、理由、可编辑话术、一个更轻的备选方案。
4. Android 显示建议卡，可复制或调用系统分享面板，但绝不后台发送。
5. 增加“帮到了 / 没帮到 / 没有联系”反馈并落库。
6. 成长事件同时记录 `own_effort` 与 `support_received`。

建议新增：

- `GET/POST/PATCH/DELETE /api/v1/support-people`
- `POST /api/v1/support/feedback`

验收：同一困境既能建议“自己试一个小步骤”，也能建议“请同学陪练”；帮助者会进入成长时间线和回望。

### P3：日、周、月回望

目标：把零散记录变成可追溯的成长故事。

1. 日总结：跨日后首次打开时生成，失败可以重试，不要求每天打卡。
2. 周总结：自己的尝试、他人的帮助、一次停顿或重新开始、下一步建议。
3. 月故事：选择 3～6 个关键节点，不只挑成功瞬间。
4. 每份总结保存 `source_event_ids`；点击节点能查看原始文字、转写或图片说明。
5. 将成长页和记录页从 `FakeConfidenceRepository` 替换为真实 repository/ViewModel 状态。
6. 空数据时显示温和空状态，不虚构内容。

建议保留现有 `GET /reviews/{period}`，再增加 `POST /api/v1/reviews/generate`，请求中包含时间范围与 `device_id`。

验收：新记录能进入当天时间线；周总结同时呈现个人努力和他人帮助。

### P4：比赛展示增强

P0～P3 完成后再做：

- 成长小片：事件选材 → AI 生成脚本 → 用户预览删改 → Media3 Transformer 本地合成 15～30 秒 MP4 → 系统分享。
- 固定叙事结构：困难 → 小尝试 → 获得帮助 → 当前变化 → 仍在继续。
- 桌面小组件增强：沿用现有 Glance 组件，改接真实事件，并增加公开许可、敏感内容过滤与可靠刷新。

## 6. 数据和安全约定

- 时间以 UTC 入库、ISO 8601 返回，Android 按本地时区分组。
- AI 派生内容保存 `model`、`prompt_version`、`source_ids`、`created_at`。
- Schema 校验失败不得写库。
- 原始照片和录音本地优先；服务端默认只保存必要转写、摘要和来源标识。
- 删除原记录时同步处理派生事件、记忆、回顾引用。
- 普通陪伴 Prompt 之外增加独立危机风险检测；命中后提供可信联系人和专业求助资源。
- 不做疾病诊断、用药建议、风险保证或自动联系他人。
- 网络失败应明确提示，不能用假成功经历冒充真实 AI 回复。

## 7. 代码入口

```text
app/src/main/java/com/testconnection/confidence_agent/
├─ data/audio                 # 录音与播放
├─ data/preferences           # 音色设置与首次画像状态
├─ data/remote                # HTTP 客户端
├─ data/repository            # 聊天、本地记录与假数据
├─ ui/screens                 # 四页、首次画像、记忆中心、语音页与封面
└─ widget                     # 三个 Glance 桌面小组件

server/app/
├─ api/routes                 # FastAPI 路由
├─ core/config.py             # 环境变量
├─ schemas                    # Pydantic 契约
└─ services                   # AI 调用与业务逻辑
```

重点文件：

- `ui/screens/HomeViewModel.kt`
- `data/remote/ChatApiClient.kt`
- `data/repository/FakeConfidenceRepository.kt`
- `data/repository/LocalRecordRepository.kt`
- `ui/screens/OnboardingScreen.kt`
- `ui/screens/RecordScreen.kt`
- `widget/DuckWidgets.kt`
- `server/app/services/chat_service.py`
- `server/app/services/multimodal_service.py`
- `server/app/schemas/`

## 8. 推荐分支与提交顺序

```text
feature/persistence-context
feature/memory-pipeline
feature/support-loop
feature/reviews
feature/growth-video-widget
```

每阶段合并前检查：后端接口测试通过；Android 至少执行 `:app:compileDebugKotlin`；没有提交密钥、数据库、构建目录、APK 或签名文件；Android DTO 与 Pydantic Schema 一致；真机走通本阶段验收故事。

## 9. 当前明确不做

- 不重画四个页面，不替换现有视觉体系。
- 不重写已经走通的文字、图片、语音和云端朗读。
- 暂不做公开社区、多设备账号、自动读取通讯录或全双工实时电话。
- 不把“语音电话页面”描述成真正实时通话；当前是轮次式语音交互。

队友优先完成 P0～P3，这四部分构成比赛项目的产品主干；你后续可以继续完善权限说明、空状态、动画、异常提示、适配和演示材料等边界体验。
